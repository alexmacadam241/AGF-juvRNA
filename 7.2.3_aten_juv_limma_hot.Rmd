---
title: "7.2.3_aten_juv_limma_hot"
output: html_document
date: "2024-08-03"
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r}
library(limma)
library(Glimma)
library(edgeR)
library(tidyverse)
set.seed(123)
```

Sample info
```{r}
coldata<- read.csv("/Users/alexmacadam/Dropbox/PhD/Chapter4_RNA/Data/Gene_counts/read_info/aten_juvenile_read_info2.csv", row.names="ID") |>
  filter(Temp == "Hot")
coldata <- coldata[,c("Cross","Temp","Symbiont","group")]
#convert columns to factors
coldata$Cross <- factor(coldata$Cross)
coldata$group <- factor(coldata$group)
coldata$Temp <- factor(coldata$Temp)
coldata$Symbiont <- factor(coldata$Symbiont)

#make sample lists for each symbiont treatment
coldata2<- coldata |> rownames_to_column(var = "Samples")
D1samples<- coldata2 |> dplyr::filter(Symbiont == "D1")
SSsamples<- coldata2 |> dplyr::filter(Symbiont == "SS")
Wildsamples<-  coldata2 |> dplyr::filter(Symbiont == "Wild")
D1samples<- D1samples$Samples
SSsamples<- SSsamples$Samples
Wildsamples<- Wildsamples$Samples
samples<- c(D1samples, SSsamples, Wildsamples)
```

Get Count Data
```{r}
x<- as.matrix(read.csv("/Users/alexmacadam/Dropbox/PhD/Chapter4_RNA/Data/Gene_counts/count_tables/Juvenile_host/all/aten_juveniles_host_all_counts2.csv", row.names = 1))
# Keep only Hotient samples
x <- x[, colnames(x) %in% samples]

#view data
#head(x,2)
rownames
rownames(coldata) <- sub("fb","", rownames(coldata))
#Do row names in coldata data match to column data in x
all(rownames(coldata) %in% colnames(x))
#are they in the same order?
all(rownames(coldata) == colnames(x))
#change order
x <- x[, rownames(coldata)]
#double check order
all(rownames(coldata) == colnames(x))
```

```{r}
#Create the matrix
x <- DGEList(counts = x)

# Assuming `sample_info` has a column named `group` for conditions
x$samples$group <- coldata$group

class(x)

dim(x)
```

```{r}
samplenames <- substring(colnames(x), 1, nchar(colnames(x)))
samplenames

colnames(x) <- samplenames

group <- as.factor(c("HotD1","HotD1","HotD1","HotD1","HotD1","HotD1","HotD1","HotD1","HotD1","HotD1","HotD1","HotD1","HotD1","HotD1","HotD1","HotD1","HotD1","HotD1","HotSS","HotSS","HotSS","HotSS","HotSS","HotSS","HotSS","HotSS","HotSS","HotSS","HotSS","HotSS","HotSS","HotSS","HotWild","HotWild","HotWild","HotWild","HotWild","HotWild","HotWild","HotWild","HotWild"))
x$samples$group <- group

Cross <- as.factor(c("J8xD13","J8xJ3","J8xJ2","J8xD1","J8xJ2","J8xD1","J8xD13","J8xD6","J8xJ2","J8xD6","J8xJ3","J8xD1","J8xD6","J8xJ3","J8xD13","J8xP5","J8xP5","J8xP5","J8xJ2","J8xJ2","J8xJ2","J8xJ3","J8xD13","J8xD13","J8xD1","J8xD1","J8xD13","J8xD6","J8xJ3","J8xD6","J8xP5","J8xP5","J8xJ2","J8xJ2","J8xJ2","J8xD1","J8xJ3","J8xD6","J8xD6","J8xJ2","J8xD1"))

x$samples$Cross <- Cross
```

```{r}
geneid <- rownames(x)
```

####
Data pre-processing

###
Transformations from the raw-scale
```{r}
cpm <- cpm(x)
lcpm <- cpm(x, log=TRUE)
```

```{r}
L <- mean(x$samples$lib.size) * 1e-6
M <- median(x$samples$lib.size) * 1e-6
c(L, M)
summary(lcpm)
```

###
Removing genes that are lowly expressed
```{r}
table(rowSums(x$counts==0)==9)

keep.exprs <- filterByExpr(x, group=group)
x <- x[keep.exprs,, keep.lib.sizes=FALSE]
dim(x)
```

```{r}
lcpm.cutoff <- log2(10/M + 2/L)
library(RColorBrewer)
nsamples <- ncol(x)
col <- brewer.pal(nsamples, "Paired")
par(mfrow=c(1,2))
plot(density(lcpm[,1]), col=col[1], lwd=2, ylim=c(0,0.26), las=2, main="", xlab="")
title(main="A. Raw data", xlab="Log-cpm")
abline(v=lcpm.cutoff, lty=3)
for (i in 2:nsamples){
den <- density(lcpm[,i])
lines(den$x, den$y, col=col[i], lwd=2)
}
legend("topright", samplenames, text.col=col, bty="n")
lcpm <- cpm(x, log=TRUE)
plot(density(lcpm[,1]), col=col[1], lwd=2, ylim=c(0,0.26), las=2, main="", xlab="")
title(main="B. Filtered data", xlab="Log-cpm")
abline(v=lcpm.cutoff, lty=3)
for (i in 2:nsamples){
den <- density(lcpm[,i])
lines(den$x, den$y, col=col[i], lwd=2)
}
legend("topright", samplenames, text.col=col, bty="n")
```

###
Normalising gene expression distributions
```{r}
x <- calcNormFactors(x, method = "TMM")
x$samples$norm.factors
```

```{r}
x2 <- x
x2$samples$norm.factors <- 1
x2$counts[,1] <- ceiling(x2$counts[,1]*0.05)
x2$counts[,2] <- x2$counts[,2]*5
```

```{r}
par(mfrow=c(1,2))
lcpm <- cpm(x2, log=TRUE)
boxplot(lcpm, las=2, col=col, main="")
title(main="A. Example: Unnormalised data",ylab="Log-cpm")
x2 <- calcNormFactors(x2)  
x2$samples$norm.factors
```

```{r}
lcpm <- cpm(x2, log=TRUE)
boxplot(lcpm, las=2, col=col, main="")
title(main="B. Example: Normalised data",ylab="Log-cpm")
```

###
Unsupervised clustering of samples
```{r}
lcpm <- cpm(x, log=TRUE)
par(mfrow=c(1,2))
col.group <- group
levels(col.group) <-  brewer.pal(nlevels(col.group), "Set1")
col.group <- as.character(col.group)
plotMDS(lcpm, labels=group, col=col.group)
title(main="A. Sample groups")
```

####
Differential expression analysis

###
Creating a design matrix and contrasts
```{r}
design <- model.matrix(~0+group+Cross)
colnames(design) <- gsub("group", "", colnames(design))
design
```

```{r}
contr.matrix <- makeContrasts(
   HotD1vsHotSS = HotD1-HotSS, 
   HotD1vsHotWild = HotD1-HotWild, 
   HotSSvsHotWild = HotSS-HotWild,
   
   levels = colnames(design))
contr.matrix
```

Removing heteroscedascity from count data
```{r}
par(mfrow=c(1,2))
v <- voom(x, design, plot=TRUE)
v
```

```{r}
vfit <- lmFit(v, design)
vfit <- contrasts.fit(vfit, contrasts=contr.matrix)
efit <- eBayes(vfit)
plotSA(efit, main="Final model: Mean-variance trend")
```

```{r}
#summary(decideTests(efit))
et <- decideTests(efit)
summary(et)
```

```{r}
#most strict filtering: log-FC that is significantly greater than 1 (equivalent to a 2-fold difference)
#tfit <- treat(vfit, lfc=1)
#dt <- decideTests(tfit)
#summary(dt)

#de.common <- which(dt[,1]!=0 & dt[,2]!=0)
#length(de.common)

#head(tfit$genes$SYMBOL[de.common], n=20)
```

Venn diagram of DEGs
```{r}
vennDiagram(et[,1:3], circle.col=c("turquoise","darkgoldenrod1", "salmon"))
```

Heatmaps
make gene list
```{r}
lcpm2<- lcpm |> as.data.frame() |> rownames_to_column(var = "Genes")

#select only genes
genes<- lcpm2 |> dplyr::select(Genes)
```

###############
SS
Examining individual DE genes from top to bottom
```{r}
HotD1.vs.HotSS <- topTreat(efit, coef=1, n=Inf)
HotD1.vs.HotWild <- topTreat(efit, coef=2, n=Inf)
HotSS.vs.HotWild <- topTreat(efit, coef=3, n=Inf)
head(HotD1.vs.HotSS)
```

```{r}
HotD1.vs.HotSS<- HotD1.vs.HotSS |> rownames_to_column(var = "gene") |> dplyr::select(gene, logFC, adj.P.Val) |> dplyr::rename(lfc = logFC, p = adj.P.Val)
#write.csv(HotD1.vs.HotSS, "/Users/alexmacadam/Dropbox/PhD/Chapter4_RNA/Data/KOG/D1.vs.HotSS.csv")

HotD1.vs.HotWild<- HotD1.vs.HotWild |> rownames_to_column(var = "gene") |> dplyr::select(gene, logFC, adj.P.Val) |> dplyr::rename(lfc = logFC, p = adj.P.Val)
#write.csv(HotD1.vs.HotWild, "/Users/alexmacadam/Dropbox/PhD/Chapter4_RNA/Data/KOG/HotD1.vs.HotWild.csv")

HotSS.vs.HotWild<- HotSS.vs.HotWild |> rownames_to_column(var = "gene") |> dplyr::select(gene, logFC, adj.P.Val) |> dplyr::rename(lfc = logFC, p = adj.P.Val)
#write.csv(HotSS.vs.HotWild, "/Users/alexmacadam/Dropbox/PhD/Chapter4_RNA/Data/KOG/HotSS.vs.HotWild.csv")
```

Useful graphical representations of differential expression results
```{r}
#D1 plot
plotMD(efit, column=1, status=et[,1], main=colnames(efit)[1], 
       xlim=c(-8,13))
```

Heatmaps
```{r}
#filter lcpm2 for only samples with each symbiont
#D1
D1_lcpm<- lcpm2 |> dplyr::select(matches(D1samples))
D1_lcpm<- cbind(genes, D1_lcpm)
D1_lcpm2<- column_to_rownames(D1_lcpm, var = "Genes")
D1_lcpm2<- data.matrix(D1_lcpm2)
```

```{r}
#D1 heatmap
library(gplots)
HotD1.vs.HotSS.2<-HotD1.vs.HotSS |> rownames_to_column(var = "Genes")
HotD1.vs.HotSS.topgenes <- HotD1.vs.HotSS.2$Genes[1:100]

#i <- which(v$genes$Genes %in% HotD1.vs.HotD1.topgenes)
i_D1 <- which(D1_lcpm$Genes %in% HotD1.vs.HotSS.topgenes)
mycol <- colorpanel(1000,"blue","white","red")

#filter top 100 genes in matrix
D1_lcpm_100<- D1_lcpm |> dplyr::filter(Genes %in% HotD1.vs.HotSS.topgenes)
D1_lcpm_100<- column_to_rownames(D1_lcpm_100, var = "Genes")
D1_lcpm_100<- data.matrix(D1_lcpm_100)

groupD1 <- as.factor(c("HotientD1","HotientD1","HotientD1","HotientD1","HotientD1","HotientD1","HotientD1","HotientD1","HotientD1","HotientD1","HotientD1","HotientD1","HotientD1","HotientD1","HotientD1","HotientD1","HotientD1","HotientD1","HotientSS","HotientSS","HotientSS","HotientSS","HotientSS","HotientSS","HotientSS","HotientSS","HotientSS","HotientSS","HotientSS","HotientSS","HotientSS","HotientSS","HotientSS","HotientSS","HotientSS","HotientSS"))

heatmap.2(D1_lcpm_100, 
   scale="row",
   #labRow=v$genes$SYMBOL[i], 
   labCol=groupD1,
   labRow="none",
   col=mycol, 
   trace="none", 
   density.info="none",
   #margin=c(8,6), 
   #lhei=c(2,10), 
   dendrogram="column"
   )
```

###############
SS
Examining individual DE genes from top to bottom
```{r}
HotSS.vs.HotSS <- topTreat(efit, coef=2, n=Inf)
head(HotSS.vs.HotSS)
```

Useful graphical representations of differential expression results
```{r}
#SS plot
plotMD(efit, column=2, status=et[,2], main=colnames(efit)[2], 
       xlim=c(-8,13))
```

Heatmaps
```{r}
#filter lcpm2 for only samples with each symbiont
#SS
SS_lcpm<- lcpm2 |> dplyr::select(matches(SSsamples))
SS_lcpm<- cbind(genes, SS_lcpm)
SS_lcpm2<- column_to_rownames(SS_lcpm, var = "Genes")
SS_lcpm2<- data.matrix(SS_lcpm2)
```

```{r}
#SS heatmap
library(gplots)
HotSS.vs.HotSS.2<-HotSS.vs.HotSS |> rownames_to_column(var = "Genes")
HotSS.vs.HotSS.topgenes <- HotSS.vs.HotSS.2$Genes[1:100]

#i <- which(v$genes$Genes %in% HotSS.vs.HotSS.topgenes)
i_SS <- which(SS_lcpm$Genes %in% HotSS.vs.HotSS.topgenes)
mycol <- colorpanel(1000,"blue","white","red")

#filter top 100 genes in matrix
SS_lcpm_100<- SS_lcpm |> dplyr::filter(Genes %in% HotSS.vs.HotSS.topgenes)
SS_lcpm_100<- column_to_rownames(SS_lcpm_100, var = "Genes")
SS_lcpm_100<- data.matrix(SS_lcpm_100)

groupSS <- as.factor(c("HotientSS", "HotSS", "HotSS", "HotientSS", "HotientSS", "HotSS", "HotientSS", "HotientSS", "HotientSS", "HotientSS", "HotSS", "HotientSS", "HotSS", "HotientSS", "HotientSS", "HotSS", "HotSS", "HotSS", "HotientSS", "HotientSS", "HotSS", "HotientSS", "HotSS", "HotientSS", "HotSS", "HotSS", "HotientSS", "HotientSS", "HotSS", "HotSS", "HotientSS", "HotientSS"))

heatmap.2(SS_lcpm_100, 
   scale="row",
   #labRow=v$genes$SYMBOL[i], 
   labCol=groupSS,
   labRow="none",
   col=mycol, 
   trace="none", 
   density.info="none",
   #margin=c(8,6), 
   #lhei=c(2,10), 
   dendrogram="column"
   )
```

###########
Wild
Examining individual DE genes from top to bottom
```{r}
HotWild.vs.HotWild <- topTreat(efit, coef=3, n=Inf)
head(HotWild.vs.HotWild)
```

Useful graphical representations of differential expreWildion results
```{r}
#Wild plot
plotMD(efit, column=3, status=et[,3], main=colnames(efit)[3], 
       xlim=c(-8,13))
```

Heatmaps
```{r}
#filter lcpm2 for only samples with each symbiont
#Wild
Wild_lcpm<- lcpm2 |> dplyr::select(matches(Wildsamples))
Wild_lcpm<- cbind(genes, Wild_lcpm)
Wild_lcpm2<- column_to_rownames(Wild_lcpm, var = "Genes")
Wild_lcpm2<- data.matrix(Wild_lcpm2)
```

```{r}
#Wild heatmap
library(gplots)
HotWild.vs.HotWild.2<-HotWild.vs.HotWild |> rownames_to_column(var = "Genes")
HotWild.vs.HotWild.topgenes <- HotWild.vs.HotWild.2$Genes[1:100]

#i <- which(v$genes$Genes %in% HotWild.vs.HotWild.topgenes)
i_Wild <- which(Wild_lcpm$Genes %in% HotWild.vs.HotWild.topgenes)
mycol <- colorpanel(1000,"blue","white","red")

#filter top 100 genes in matrix
Wild_lcpm_100<- Wild_lcpm |> dplyr::filter(Genes %in% HotWild.vs.HotWild.topgenes)
Wild_lcpm_100<- column_to_rownames(Wild_lcpm_100, var = "Genes")
Wild_lcpm_100<- data.matrix(Wild_lcpm_100)

groupWild <- as.factor(c("HotWild", "HotientWild", "HotientWild", "HotWild", "HotientWild", "HotientWild", "HotWild", "HotientWild", "HotientWild", "HotientWild", "HotWild", "HotientWild", "HotWild", "HotientWild", "HotientWild", "HotientWild", "HotientWild", "HotWild", "HotWild", "HotWild", "HotientWild", "HotientWild", "HotWild"))

heatmap.2(Wild_lcpm_100, 
   scale="row",
   #labRow=v$genes$SYMBOL[i], 
   labCol=groupWild,
   labRow="none",
   col=mycol, 
   trace="none", 
   density.info="none",
   #margin=c(8,6), 
   #lhei=c(2,10), 
   dendrogram="column"
   )
```
